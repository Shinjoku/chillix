import Carousel from "../Carousel";

const Page = () => (
  <>
    {/* <Profile></Profile> */}
    <Carousel type="highlight" items={[]}></Carousel>
    <Carousel type="standard" items={[]}></Carousel>
  </>
);

export default Page;
